// Next.js API route support: https://nextjs.org/docs/api-routes/introduction.

// Request and response in NextJS default API route.

export default function handler(req, res) {
  res.status(200).json({
    example: "create your own routes"
  })
}

// Acess in https://yourdomain/api/example.
// Result in JSON.
